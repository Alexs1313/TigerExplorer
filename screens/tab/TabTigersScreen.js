import {StyleSheet, Text, View} from 'react-native';

const TabTigersScreen = () => {
  return (
    <View style={styles.container}>
      <Text>TabTigersScreen</Text>
    </View>
  );
};

const styles = StyleSheet.create({
  container: {
    backgroundColor: '#red',
    alignItems: 'center',
    justifyContent: 'center',
  },
});

export default TabTigersScreen;
